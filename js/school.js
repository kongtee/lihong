require('../assets/css/school.less');

import ChangeTab from '../common/changeTab';

new ChangeTab({
    headTab: '#introHeader',     //填写整个头部tab的最外层元素id名
    bodyTab: '#introBody'      //填写整个内容tab的最外层元素id名
});